{
	"name": "acp-mock-server",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"description": "A fully functional ACP v1 mock agent server for client testing.",
	"scripts": {
		"build": "tsc -p tsconfig.json",
		"start:tcp": "node dist/src/tcp.js",
		"test": "node --test dist/tests/*.test.js",
		"typecheck": "tsc --noEmit -p tsconfig.json"
	},
	"devDependencies": {
		"@types/node": "^20.19.0",
		"typescript": "^5.7.2"
	}
}
