name: Release

on:
  workflow_dispatch:

jobs:
  deferred:
    name: Native release automation deferred
    runs-on: macos-14
    steps:
      - name: Deferred to issue 23
        run: |
          echo "Native signing, notarization, archive/export, DMG packaging, and Homebrew release automation are intentionally deferred to issue #23."
          echo "This workflow is inert and does not publish artifacts."
